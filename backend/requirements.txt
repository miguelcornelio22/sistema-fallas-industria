# ================================================================
# requirements.txt — Backend FastAPI
# Proyecto Final — Hidrocarburos del Perú
# SIN pinado de versiones de langchain — pip las resuelve solo
# ================================================================

# ── API ───────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0

# ── LangChain — sin pinar, pip resuelve las versiones compatibles ─
langchain
langchain-openai
langchain-elasticsearch
langchain-community
langgraph

# ── LLM ──────────────────────────────────────────────────────────
openai>=1.0.0

# ── Elasticsearch ─────────────────────────────────────────────────
# NO pinar — langchain-elasticsearch define la versión compatible
elasticsearch

# ── PDF ───────────────────────────────────────────────────────────
fpdf2>=2.7.0

# ── Datos ─────────────────────────────────────────────────────────
pandas>=2.0.0
numpy<2.0
