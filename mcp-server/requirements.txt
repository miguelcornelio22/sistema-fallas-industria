# ================================================================
# requirements.txt — MCP Server Gestión de Fallas
# Proyecto Final — Hidrocarburos del Perú
# Target: Google Cloud Run (Python 3.12, linux/amd64)
#
# NOTA: elasticsearch NO se pina aquí.
# langchain-elasticsearch 1.0.0 requiere elasticsearch>=8.19.0
# Si se pina a 8.17.0 hay conflicto de dependencias → BUILD FAILURE
# Dejar que pip resuelva la versión correcta automáticamente.
# ================================================================

# ── Servidor MCP ──────────────────────────────────────────────────
fastmcp==2.14.7

# ── RAG + Embeddings ──────────────────────────────────────────────
langchain-core==1.4.9
langchain-openai==1.3.5
langchain-elasticsearch==1.0.0
# elasticsearch se instala automáticamente como >=8.19.0

# ── Generación de PDF ─────────────────────────────────────────────
fpdf2==2.8.7

# ── Datos ─────────────────────────────────────────────────────────
pandas==2.2.3
